node_modules
web
.mnist-cache
tsconfig.tsbuildinfo
*.local

.playwright-mcp
/attract-*.png
/*.png

# Only the GENERATED public/ assets (staged by npm install) — a blanket
# public/ would silently untrack any real static asset added later.
public/mediapipe-wasm/
public/models/
